version: '2'
services:
  zitech-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/zitech/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=zitech
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
